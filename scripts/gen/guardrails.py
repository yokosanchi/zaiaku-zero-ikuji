"""公開前のセーフティネット。

- check_banned: CLAUDE.md の禁止語（説教調）を検出
- check_ymyl : 医療・安全上あぶない断定を検出 → 見つかったら公開せず REVIEW.md 行き
どちらもヒューリスティック。取りこぼし・誤検知はあり得るが、
「あやしければ人間に回す」方向に倒してある。
"""

from __future__ import annotations

import re

BANNED_PATTERNS: list[tuple[str, str]] = [
    (r"す?べき(?:です|だ|でしょう|だろう|)", "〜すべき"),
    (r"して(?:ください|下さい)", "〜してください"),
    (r"しなきゃ(?:ダメ|だめ|いけない)", "〜しなきゃダメ"),
    (r"がんばりましょう|頑張りましょう|(?:がんばって|頑張って)(?:ください|下さい)", "がんばりましょう"),
    # 極端・乱暴な表現（ガバナンス方針 §3）
    (r"育児(?:は|なんて|って|なんか)?\s*(?:適当|てきとう|テキトー)で\s*(?:いい|OK|大丈夫|オッケー)", "育児は適当でいい"),
    (r"(?:放置|ほったらかし|ほっとけ|ほっといて)(?:して?も?|でも?)?\s*(?:いい|大丈夫|平気|育つ|問題ない|OK)", "放置でいい/放置しても育つ"),
    (r"子ども?を?\s*放っておいても?\s*(?:いい|大丈夫|平気|育つ)", "放っておいてもいい"),
]

# (ラベル, 必須語, 文脈語(any/空=不問), 危険語(any/空=不問))
YMYL_RULES: list[tuple[str, list[str], list[str], list[str]]] = [
    (
        "1歳未満のはちみつ",
        [r"はちみつ|ハチミツ|蜂蜜"],
        [r"0歳|ゼロ歳|1歳(?:未満|前|になる前)|生後|乳児|赤ちゃん|離乳"],
        [r"与え|あげ|食べさせ|なめさせ|使っ|混ぜ|おすすめ|良い|よい|大丈夫|平気"],
    ),
    (
        "うつ伏せ寝の推奨",
        [r"うつ(?:伏せ|ぶせ)"],
        [r"寝|睡眠|寝かせ|寝かし"],
        [r"推奨|おすすめ|良い|よい|安心|大丈夫|問題ない|平気|させて"],
    ),
    (
        "自己判断の断薬・通院中止",
        [r"薬をやめ|断薬|服薬をやめ|通院をやめ|受診をやめ|病院に行かなく"],
        [],
        [r"大丈夫|問題ない|いい|平気|必要ない|しなくて|やめても"],
    ),
    (
        "発熱・けいれんの様子見断定",
        [r"高熱|発熱|けいれん|痙攣|ひきつけ|意識|ぐったり|脱水"],
        [],
        [r"様子見で(?:いい|大丈夫|平気)|放って?おいて|受診しなくて(?:いい|大丈夫)|病院に行かなくて(?:いい|大丈夫)"],
    ),
    (
        "予防接種の忌避",
        [r"ワクチン|予防接種"],
        [],
        [r"打たなくて(?:いい|大丈夫)|不要|受けなくて(?:いい|大丈夫)|危険だから避け"],
    ),
    (
        "添い寝・同床を安全と断定",
        [r"添い寝|添い乳|同じ布団|ベッドで一緒|同床"],
        [],
        [
            r"(添い寝|添い乳|同じ布団|一緒に寝て|同床)(?:は|も|でも|しても|なら)?\s*"
            r"(安全|安心|問題ない|大丈夫|推奨|おすすめ|心配ない)"
        ],
    ),
]


def check_banned(text: str) -> list[str]:
    hits = [label for pat, label in BANNED_PATTERNS if re.search(pat, text)]
    return sorted(set(hits))


# 「危険を避けよう」という文脈は安全側なので、危険語が近くにあっても除外する
_SAFE_NEGATION = re.compile(
    r"避け|さけ|しない|防ぐ|防止|危険|リスク|注意|控え|やめ|ではなく|NG|禁物|絶対に(?!安全)"
)


def _sentences(text: str) -> list[str]:
    return re.split(r"(?<=[。！？\n])", text)


# ガバナンス方針 §2：「Y を扱うなら X も併記」の"対で書く"ヒューリスティック。
# 片方だけのときは REVIEW.md に警告（公開は止めない）。
# trigger は「子に画面を頼る／市販に頼る」という“依存の勧め”の文脈だけを狙う
# （SNSのバズ動画に言及しただけ、では発火させない）。
_GOVERNANCE_RULES: list[tuple[str, str, str]] = [
    (
        "デジタル: 時間のメリハリ／作品選定のルールに触れていない",
        # 「子に画面を頼る」と読める依存フレーズだけを狙う（バズ動画への言及では発火させない）
        r"(?:スマホ|動画|YouTube|ユーチューブ|テレビ|ＴＶ|タブレット|アニメ)(?:に|を|で|の力を)?"
        r"(?:[^。！？\n]{0,12})?"
        r"(?:頼(?:っ|る|り|ら)|助けてもら|見せて(?:おく|おけ|しのぐ|やり過ご|乗り切|休|おい)|見せ続け|見せている(?:間|うち)|静かにさせ|子守(?:り)?(?:を|に)|お守り(?:を|に)代わ)",
        r"\d+\s*分|時間(?:や[^。！？\n]{0,6})?を?(?:ゆるく)?決め|時間を区切|メリハリ|ルールを決め|"
        r"見る(?:作品|もの|番組)を?(?:ゆるく)?(?:決め|選)|作品を選|一日\s*\d|上限|だらだら見せ(?:ない|ず)|区切っ|「ここまで」",
    ),
    (
        "食事: 栄養を補う簡単な工夫（汁物・果物など）に触れていない",
        r"(?:レトルト|惣菜|そうざい|お惣菜|市販(?:の)?(?:ベビーフード|離乳食|冷凍)|ベビーフード|冷凍食品|買った(?:もの|ごはん))"
        r"(?:[^。！？\n]{0,24})?"
        r"(?:フル活用|に頼|を頼|で済ま|で乗り切|でいい|だけで|活用して|まかせ|任せ)",
        r"栄養(?:は|バランス|面)|汁物|みそ汁|味噌汁|スープ|野菜|果物|フルーツ|バランス|補(?:う|える|って|完)|添え|プラス一品|もう一品",
    ),
    (
        "安全: 室内事故対策（ゲート・モニター等）が前提だと明記していない",
        r"別室で(?:休|寝|過ご)|目を離(?:す|し)て(?:も)?(?:いい|大丈夫|平気|休)|家事に没頭|子どもを一人にして(?:休|家事|寝)|放っておいて(?:休|家事)|そばを離れて(?:休|寝|家事)",
        r"ベビーゲート|ベビーモニター|モニター|柵|さく|安全対策|危険なものを(?:片づけ|しま|どけ)|手の届か(?:ない|ぬ)|誤飲|転落|溺れ|チャイルドロック|安全が確保",
    ),
]


def check_governance(text: str) -> list[str]:
    """『対で書く』ルールの未充足を警告として返す（非ブロッキング）。"""
    out: list[str] = []
    for label, trigger, paired in _GOVERNANCE_RULES:
        if re.search(trigger, text) and not re.search(paired, text):
            out.append(label)
    return out


def check_ymyl(text: str) -> list[str]:
    flags: list[str] = []
    sents = _sentences(text)
    for label, must_any, ctx_any, danger_any in YMYL_RULES:
        if not any(re.search(p, text) for p in must_any):
            continue
        hit = False
        for s in sents:
            if not any(re.search(p, s) for p in must_any):
                continue
            if ctx_any and not any(re.search(p, s) for p in ctx_any):
                continue
            if danger_any and not any(re.search(p, s) for p in danger_any):
                continue
            # 同じ文が「避ける／防ぐ／危険」等を含むなら安全側とみなす
            if _SAFE_NEGATION.search(s):
                continue
            hit = True
            break
        if hit:
            flags.append(label)
    return flags
